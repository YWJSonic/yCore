package counttool

import (
	"testing"
)

func Test_NewCounePreDuration(t *testing.T) {
	// handle := NewCounePreDuration(time.Second * 2)

	// for i := 0; i < 120; i++ {
	// 	go func() {
	// 		for {
	// 			count := handle.GetCount()
	// 			fmt.Println(count)
	// 			if count > 800 {
	// 				time.Sleep(time.Minute)
	// 			}
	// 			handle.Incr()
	// 			time.Sleep(time.Millisecond * 200)
	// 		}
	// 	}()
	// }
	// select {}
}
